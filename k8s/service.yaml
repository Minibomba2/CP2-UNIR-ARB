apiVersion: v1
kind: Service
metadata:
  name: aks-web-service
  namespace: cp2
spec:
  type: LoadBalancer
  selector:
    app: aks-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
